Pull-out setup code for shaders from RenderSystem to Engine
    - an update functor must be created for directionalLight.cpp and pointLight.cpp

Load model colours

Clean up dll interfaces to engine

Add colour class

Rename glsl (shader files) "color" -> "colour"! (and code)

Create component system, that's where node.h will live
This should live in the Managers.dll