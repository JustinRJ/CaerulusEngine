Add colour class

Rename glsl (shader files) "color" -> "colour"! (and code)

Set m_UniformLocations in ShaderSource via reading file

Create component system, that's where node.h will live
This should live in the Managers.dll

Load shaders into managers